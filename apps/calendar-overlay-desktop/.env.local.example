# Google Cloud Console 의 "데스크톱 앱" 유형 OAuth client ID (필수).
# PKCE 로 보호되므로 client secret 은 보통 비워둔다 (Google 이 요구할 때만 채움).
# 이 파일을 .env.local 로 복사해 값을 채우면 빌드 시 번들에 주입된다.
GOOGLE_CLIENT_ID=your-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=
